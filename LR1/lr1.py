# void lev() {
#     float h, s, x, res;
#     h = (b - a) / n;
#     s = 0;
#     x = a;
#     while (x <= (b - h))
#     {
#       s += fun(x);
#       x += h;
#     }
#     res = h * s;
#     printf("\n > Результат методом левых частей:  %.4f\n", res);
#      float r = 0, m = 0;
#  if (pro(a) > pro(b)) {
#    m = fabs(pro(a));
#  }
#  else{
#    m = fabs(pro(b));
#  }
#  r = (pow(b - a, 2)/(2*n))*m;
#  printf(" > Остаточный член равен: %f\n", r);
#    secondMenu();
# }

